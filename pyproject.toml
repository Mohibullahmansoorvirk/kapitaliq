[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kapitaliq"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["agents*", "pipelines*", "api*"]